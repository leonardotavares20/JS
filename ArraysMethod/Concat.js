//Concat junta dois arrays, ele nao altera os arrays originais, so gera um novo array com os elementos do array
//Ele pode concatenar mais de um array

const foods = ["Banana", "Apple", "Orange"];

const numbers = [10, 20, 30, 40, 50];

const arraysConcatenated = foods.concat(numbers);

console.log(arraysConcatenated);
